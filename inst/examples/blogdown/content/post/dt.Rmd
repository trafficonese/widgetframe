---
title: "DT + widgeframe"
author: "Bhaskar V. Karambelkar"
date: 2017-08-06T11:45:00-05:00
categories: ["widgetframe"]
tags: ["DT"]
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(collapse = TRUE)
```

The two examples below show how to properly embed a `DT` widget inside a blog post using the `widgetframe` package.

## DT

```{r dt-01}
library(DT)
library(widgetframe)
dt <-  datatable(
  head(iris, 20), 
  options = list(
     columnDefs = list(list(className = 'dt-center', targets = 5)),
     pageLength = 5, lengthMenu = c(5, 10, 15, 20)),
  fillContainer = T)

frameWidget(dt, height = 350, width = '95%')
```
